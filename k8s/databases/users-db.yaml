apiVersion: v1
kind: Secret
metadata:
  name: users-db-secret
  namespace: fcg
type: Opaque
stringData:
  ACCEPT_EULA: "Y"
  MSSQL_SA_PASSWORD: Fcg@12345
  MSSQL_PID: Developer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-db
  namespace: fcg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-db
  template:
    metadata:
      labels:
        app: users-db
    spec:
      containers:
        - name: users-db
          image: mcr.microsoft.com/mssql/server:2022-latest
          ports:
            - containerPort: 1433
          envFrom:
            - secretRef:
                name: users-db-secret
---
apiVersion: v1
kind: Service
metadata:
  name: users-db
  namespace: fcg
spec:
  selector:
    app: users-db
  ports:
    - port: 1433
      targetPort: 1433
